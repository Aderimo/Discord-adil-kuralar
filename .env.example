# Veritabanı Bağlantısı
# Geliştirme için SQLite (lokal)
# DATABASE_URL="file:./dev.db"

# Production için PostgreSQL (Neon - Netlify'de bu kullanılacak)
DATABASE_URL="postgresql://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require"

# JWT Secret (Authentication için - güçlü bir secret kullanın!)
JWT_SECRET="your-jwt-secret-here-change-this-in-production"

# OpenAI API Key (AI Danışman için - opsiyonel)
# OPENAI_API_KEY="sk-..."

# Site URL (Netlify deploy sonrası güncellenecek)
# NEXT_PUBLIC_SITE_URL="https://your-site.netlify.app"
